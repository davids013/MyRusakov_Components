<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a5_androidcomponents">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.5_AndroidComponents">
        <activity android:name=".L6_SpinnerActivity"></activity>
        <activity android:name=".L5_2" />
        <activity android:name=".L5_RadioButtonActivity" />
        <activity android:name=".L4_CheckBoxActivity" />
        <activity android:name=".L3_ButtonActivity" />
        <activity android:name=".L2_EditTextActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>